//comment here
add main_lib
local simple_text = "Hello world"
local sample_path = "C:\test\test.txt"

func Test(arg)
        main_lib.print(arg, arg)
end

func Entry()
        main_lib.print(simple_text)
        local test = main_lib.newFile(sample_path, "{"test": "Hello world!"}")
        Test(test)
        return SUCCESS_EXIT
end