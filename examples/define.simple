/*
* !define is powerful since you can set values that the VM uses such as MIN_VERSION and MIN_RAM
* for MIN_VERSION the format MUST be vX.X.X
* for MIN_RAM the format is in MB so if we did 1GB it would be 1024
* GB support will be coming soon
*/

!define MIN_RAM 256
!define MIN_VERSION v1.0.0
!define MAX_VERSION v1.0.1

print->("Should work!")